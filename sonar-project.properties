sonar.projectKey=Phalcode_gamevault-backend
sonar.organization=phalcode
sonar.sources=src

# Migrations are DB migration scripts, not production code; exclude them so they
# do not affect code-quality metrics (duplication, coverage, complexity).
sonar.exclusions=**/migrations/**